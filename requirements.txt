# VisionAI direct dependencies, pinned to the versions of the validated
# environment (Python 3.11, Windows, CPU-only torch).
# NOTE: the --extra-index-url line means a fresh `pip install` needs network
# access. For the OFFLINE factory machine, build a wheelhouse on a connected
# machine first:  pip download -r requirements.txt -d wheelhouse
# then install with:  pip install --no-index --find-links wheelhouse -r requirements.txt
# For a fully reproducible freeze of the validated environment, also ship
# `pip freeze > requirements-frozen.txt` next to this file.
--extra-index-url https://download.pytorch.org/whl/cpu

anomalib==2.4.2
torch==2.12.0
torchvision==0.27.0
ultralytics==8.4.52
PySide6==6.11.1
numpy==2.4.6
opencv-python==4.13.0.92
Pillow==12.2.0
mss==10.2.0
PyGetWindow==0.0.9
safetensors==0.7.0

# Test-only dependency (not needed to run the applications).
pytest==8.3.4
